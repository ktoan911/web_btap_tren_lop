version: '3'
services:
  mongodb:
    image: mongo:latest
    container_name: student-mongo
    ports:
      - "27018:27017"
    volumes:
      - student_mongo_data:/data/db

volumes:
  student_mongo_data:
